apiVersion: v1
kind: Service

metadata:
  name: nginx
  annotations:
    argocd.argoproj.io/sync-wave: "0"

spec:
  selector:
    app: nginx

  ports:
  - port: 80
    targetPort: 80

  type: ClusterIP